name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_16.2.app

    - name: Build
      run: xcodebuild build -scheme SwiftConfettiView -destination 'generic/platform=iOS Simulator'

    - name: Test
      run: xcodebuild test -scheme SwiftConfettiView -destination 'platform=iOS Simulator,name=iPhone 16'

  spm:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_16.2.app

    - name: Build (SPM)
      run: xcodebuild build -scheme SwiftConfettiView -destination 'generic/platform=iOS Simulator'
